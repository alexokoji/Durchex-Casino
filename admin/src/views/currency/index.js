import React from 'react';
import { Box, Button, Typography } from '@mui/material';

const CurrencyManagement = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h6">Currency Management (placeholder)</Typography>
      <Button variant="contained" sx={{ mt: 2 }}>Add New Currency</Button>
    </Box>
  );
};

export default CurrencyManagement;
